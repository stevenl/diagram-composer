# Single source of truth for the project version.
#
# Gradle applies `version` and `group` from this file to the root project
# automatically; build.gradle.kts propagates them to all subprojects.
#
# This line is rewritten automatically by release-please as part of
# .github/workflows/release.yml — see docs/development.md §19. Do not bump
# it by hand.
version=0.0.0
group=dev.diagramcomposer

kotlin.code.style=official
