package dev.diagramcomposer.core

import org.junit.jupiter.api.Assertions.assertTrue
import org.junit.jupiter.api.Test

/**
 * Confirms the `core` module's JUnit 5 test setup is wired correctly.
 * Replace/remove once real domain-model tests land in Milestone 1.
 */
class PlaceholderTest {

    @Test
    fun `module test setup compiles and runs`() {
        assertTrue(true)
    }
}
