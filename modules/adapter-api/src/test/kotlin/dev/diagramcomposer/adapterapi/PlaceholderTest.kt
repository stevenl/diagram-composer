package dev.diagramcomposer.adapterapi

import org.junit.jupiter.api.Assertions.assertTrue
import org.junit.jupiter.api.Test

/**
 * Confirms the `adapter-api` module's JUnit 5 test setup is wired correctly.
 * Replace/remove once the DiagramAdapter contract tests land in Milestone 2.
 */
class PlaceholderTest {

    @Test
    fun `module test setup compiles and runs`() {
        assertTrue(true)
    }
}
