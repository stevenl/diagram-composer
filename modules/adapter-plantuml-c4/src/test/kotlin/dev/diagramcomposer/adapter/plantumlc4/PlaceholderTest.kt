package dev.diagramcomposer.adapter.plantumlc4

import org.junit.jupiter.api.Assertions.assertTrue
import org.junit.jupiter.api.Test

/**
 * Confirms the `adapter-plantuml-c4` module's JUnit 5 test setup is wired
 * correctly. Replace/remove once real parser tests land in Milestone 3.
 */
class PlaceholderTest {

    @Test
    fun `module test setup compiles and runs`() {
        assertTrue(true)
    }
}
