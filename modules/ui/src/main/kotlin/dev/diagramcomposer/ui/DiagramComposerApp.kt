package dev.diagramcomposer.ui

import androidx.compose.runtime.Composable

/**
 * Root composable for the Diagram Composer UI.
 *
 * Intentionally empty at this stage (Milestone 0 scaffolding). Real content
 * (element/relationship views, editing flows, source sync) lands in
 * Milestones 7-9.
 */
@Composable
fun diagramComposerApp() {
    // No content yet — scaffolding only.
}
